import reducer from './reducer';

export { default as channelsTypes } from './types';
export { default as channelsActions } from './actions';
export { default as channelsSelectors } from './selectors';

export default reducer;
